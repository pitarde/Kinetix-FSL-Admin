VITE_EMAILJS_SERVICE_ID=your_service_id
VITE_EMAILJS_TEMPLATE_ID=your_template_id
VITE_EMAILJS_PUBLIC_KEY=your_public_key

# Cloudflare R2 media cleanup (account deletion). Optional — src/lib/r2.js
# falls back to the same values baked into the mobile app, so leave these
# unset unless the Worker URL or shared secret changes.
VITE_UPLOAD_WORKER_URL=https://kinetix-upload.pitardeken2024.workers.dev
VITE_R2_DELETE_SECRET=kinetix-delete-2026
